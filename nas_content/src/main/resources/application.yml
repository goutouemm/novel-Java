server:
  port: 10087
spring:
  application:
    name: nas-content #指定服务名
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/novel_system?serverTimezone=GMT
    username: root
    password: 123456